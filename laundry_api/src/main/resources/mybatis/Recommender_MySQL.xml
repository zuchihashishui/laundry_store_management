<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Recommender">
	
	<select id="getRecommendsByUserId" 
		parameterType="java.lang.Integer"
		resultType="java.util.LinkedHashMap">
		SELECT RECOMMENDER_ID AS recommenderId
			, RECOMMENDER_NAME AS recommenderName
			, PHONE_NUMBER AS phoneNumber
			, ADDRESS AS address
			, NOTE AS note
			, REGIST_PHONE_NUMBER AS registPhoneNumber
		FROM RECOMMENDER
		WHERE USER_ID = #{userId}
	</select>
	
	<insert id="insert" parameterType="java.util.Map">
		INSERT INTO RECOMMENDER(
			RECOMMENDER_NAME
			, PHONE_NUMBER
			, ADDRESS
			, NOTE
			, REGIST_PHONE_NUMBER
		)
		VALUES(
			#{recommenderName}
			, #{phoneNumber}
			, #{address}
			, #{note}
			, #{registPhoneNumber}
		)
	</insert>

</mapper>